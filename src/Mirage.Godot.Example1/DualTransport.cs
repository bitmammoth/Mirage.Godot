using Godot;
using Mirage.Godot.Scripts.Udp;
using System;

public partial class DualTransport : Node
{
	/*public override void _Ready()
	{
		var udpSocketFactory = new UdpSocketFactory();
		var websocketFactory = new Factory

		var udpTransport = udpSocketFactory.CreateSocket();
		var websocketTransport = websocketFactory.CreateSocket();

		var multipassTransport = new MultipassTransport(udpTransport, websocketTransport);

		networkManager.SetupSocketFactory(multipassTransport);
	}*/
}
